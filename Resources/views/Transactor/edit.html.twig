<h1>Edit Transactor</h1>

<form action="{{ path('admin_transactor_update', { 'id': transactor.id }) }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p class="form-buttons">
        <button type="submit">Save Changes</button>
    </p>
</form>
